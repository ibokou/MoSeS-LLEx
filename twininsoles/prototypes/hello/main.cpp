#include <iostream>
#include <memory>
#include <vector>
#include <stdio.h>
#include <memory>

auto main() -> int
{
    std::vector<int> list;
    list.push_back(24);
    list.push_back(29);
    auto ibo = std::vector<int>{2, 3, 4, 5, 6};
    auto rasengan = std::string("hallo");
    rasengan.append("HALO");

    printf("my tag : %s", tag.get());

    return 0;
}