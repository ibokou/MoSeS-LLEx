import 'package:flutter/material.dart';

class TopologyPage extends StatelessWidget {
  static const title = "Topology";

  const TopologyPage({super.key});

  @override
  Widget build(BuildContext context) => Scaffold(
        body: Center(
            child: Icon(
          Icons.hive,
        )),
      );
}
